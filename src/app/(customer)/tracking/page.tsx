import { ComingSoon } from "@/components/ui";

/**
 * Customer order tracking (C-40+). Stub until the tracking screen is built —
 * exists so the "تتبع الطلب" bottom-nav tab resolves instead of 404-ing.
 */
export default function TrackingPage() {
  return <ComingSoon title="تتبع الطلب" />;
}
