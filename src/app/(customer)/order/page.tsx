import { ComingSoon } from "@/components/ui";

/**
 * Customer order flow (C-20+). Stub until the ordering screens are built — exists
 * so the home CTA and the "اطلب الان" bottom-nav tab resolve instead of 404-ing.
 */
export default function OrderPage() {
  return <ComingSoon title="اطلب الان" />;
}
