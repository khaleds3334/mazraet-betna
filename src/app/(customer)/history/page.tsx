import { ComingSoon } from "@/components/ui";

/**
 * Customer past orders (C-50+). Stub until the history screen is built — exists
 * so the home "الطلبات السابقة" button and the sidebar link resolve instead of
 * 404-ing.
 */
export default function HistoryPage() {
  return <ComingSoon title="الطلبات السابقة" />;
}
