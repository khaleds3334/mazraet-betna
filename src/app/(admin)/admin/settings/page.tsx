/**
 * Admin settings (A-70). Stub until Phase 5's settings screen is built — exists
 * so the header gear on the home resolves and the routing is real.
 */
export default function AdminSettingsPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 px-screen text-center">
      <h1 className="text-h5 font-bold text-heading">الإعدادات</h1>
      <p className="text-muted">الصفحة قيد الإنشاء…</p>
    </main>
  );
}
