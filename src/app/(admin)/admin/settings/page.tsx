import { ComingSoon } from "@/components/ui";

/**
 * Admin settings (A-70). Stub until Phase 5's settings screen is built — exists
 * so the header gear on the home resolves and the routing is real.
 */
export default function AdminSettingsPage() {
  return <ComingSoon title="الإعدادات" />;
}
