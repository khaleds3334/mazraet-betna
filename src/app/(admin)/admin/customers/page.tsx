import { ComingSoon } from "@/components/ui";

/**
 * Admin customers (A-30). Stub until Phase 5's customer list is built — exists so
 * the bottom-nav link resolves and the routing is real.
 */
export default function AdminCustomersPage() {
  return <ComingSoon title="العملاء" />;
}
