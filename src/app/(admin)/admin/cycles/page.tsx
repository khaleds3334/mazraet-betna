/**
 * Admin cycles (A-40 → A-47). Stub until Phase 5's cycles area is built — exists
 * so the bottom-nav link and the "ابدأ سجل اول دورة" button on the home resolve.
 */
export default function AdminCyclesPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 px-screen text-center">
      <h1 className="text-h5 font-bold text-heading">الدورات</h1>
      <p className="text-muted">الصفحة قيد الإنشاء…</p>
    </main>
  );
}
