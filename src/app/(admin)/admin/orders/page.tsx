/**
 * Admin orders (A-50). Stub until Phase 5's orders list is built — exists so the
 * bottom-nav link resolves and the routing is real.
 */
export default function AdminOrdersPage() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center gap-2 px-screen text-center">
      <h1 className="text-h5 font-bold text-heading">الطلبات</h1>
      <p className="text-muted">الصفحة قيد الإنشاء…</p>
    </main>
  );
}
