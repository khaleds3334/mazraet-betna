# حالة التنفيذ — مزرعة بيتنا

> **لـ Claude Code:** اقرا الملف ده في بداية كل جلسة، وحدّثه في آخرها. ده مصدر الحقيقة الوحيد لـ "إحنا واقفين فين".

**آخر تحديث:** ١٩ أغسطس ٢٠٢٦
**المرحلة الحالية:** المرحلة ٥ — تطبيق الأدمن (+ المرحلة ٨ جزء ١: التطبيق بقى يتثبّت على الموبايل) · خلص: رئيسية الأدمن A-10 + شِل الأدمن + تسجيل الدورة A-41 + الدورات فاضية A-40 + لوحة التربية A-11 + المصاريف A-15 + سحب الشكارة A-13 + لوحة البيع A-20 + شاشة الطلبات A-50 + نافذة اضافة طلب A-56 + كارت الطلب الجديد + إلغاء الطلب A-51 + **شاشة العملاء A-30 + صف العميل + تسجيل/تعديل العميل A-34/A-35** + **شاشات التحميل لكل مسارات الأدمن**

---

## ✅ خلص

**المرحلة ٠ — التجهيز:**
- [x] مشروع Next.js ١٦ (App Router + TypeScript + Tailwind v4 + src/)
- [x] هيكل المجلدات كامل زي BUILD-WORKFLOW
- [x] خط Almarai محلي (٤ أوزان) + اتجاه RTL + `lang="ar"` في الـ layout
- [x] مكتبة الأيقونات Hugeicons متركّبة
- [x] `.env.local` متوصّل بمشروع Supabase (المفاتيح جاهزة)
- [x] `pnpm build` بيعدّي بنجاح

> **ملاحظة تقنية:** الـ scaffold نزّل Tailwind v4 و Next 16 (أحدث من المكتوب). التوكنز دلوقتي في `globals.css` مش `tailwind.config.ts`. التفاصيل في القرار T-11.

**المرحلة ١ — قاعدة البيانات:**
- [x] الـ schema كامل (١٢ جدول) — `farm · admin_credentials · settings · cycle · customer · orders · order_line · payment · expense · feed · mortality · notification`
- [x] `farm_id` على كل جدول (متعدّد المزارع من أول يوم — D-08)
- [x] `orders.customer_id` قابل للـ null (الطلب اليتيم — FR-13)
- [x] `order_line` = فرخة واحدة، بتحمل الوزن التقريبي **والفعلي** (D-13)
- [x] `order_line.batch_no` لتقسيم الوزنات (FR-14ب)
- [x] مفيش جدول فاتورة ولا دين — محسوبين على القراءة (D-05 / T-15)، والسعر بيتثبّت على الطلب وقت الوزن
- [x] `payment` بيدعم الدفع على دفعات (FR-17)
- [x] PIN الأدمن مشفّر في جدول منفصل `admin_credentials` (T-14)
- [x] RLS: العميل يشوف بياناته بس، الأدمن يشوف كله (T-13) — كل الجداول متأمّنة
- [x] بيانات تجريبية واقعية (دورة نشطة + ٥ عملاء + ٧ طلبات في كل الحالات + مصاريف/علف/نفوق/إشعارات)
- [x] أنواع TypeScript متولّدة في `src/types/database.ts`
- [x] فحص الأمان (advisors) نضيف — كل تحذير اتظبط، وباقي INFO واحد مقصود (`admin_credentials` deny-all)

> ملفات الهجرة محفوظة في `supabase/migrations/` (`001_schema` · `002_rls` · `003_seed`).
> **PIN الأدمن التجريبي = ١٢٣٤٥٦** (للتطوير بس).

---

## ✅ خلص — المرحلة ٢ (الأساسيات)

**منطق خالص (بدون تصميم):**
- [x] `lib/format.ts` — أرقام عربية · جنيه · وزن (٣ خانات) · جمع الفراخ · تاريخ · وقت · تليفون
- [x] `lib/constants.ts` — مدة التربية · جدول الحرارة · مسميات الحالات (بتختلف حسب العميل/الأدمن) · تبويبات الطلبات · أنواع المصاريف
- [x] `lib/calculations/` — `invoice` (الفاتورة من الطلب) · `cycle` (العمر/الحرارة/النفوق/الربح) · `feed` (العلف)

**من التصميم:**
- [x] توكنز Figma → `globals.css` تحت `@theme` (ألوان + خطوط + ظلال — T-16)
- [x] `lib/icons.ts` + `Icon.tsx` — خريطة أيقونات مركزية متحقّق منها؛ بتكبر مع كل شاشة
- [x] عملاء Supabase — `client` (متصفح) · `server` (خادم + عميل خدمة للـ PIN) · `middleware` (تحديث الجلسة)
- [x] نظام الـ Toast — `useToast` (مخزن pub/sub) · `Toast` · `Toaster` · `InlineError` (للعمليات الحرجة)

> **مكوّنات `components/ui`:** بنبنيها **شاشة بشاشة** وإحنا ماشيين (قرار خالد)، مش دفعة واحدة.
> **باقي للمرحلة ٣:** تركيب `<Toaster/>` في layouts (customer)/(admin) + التأكد بصرياً من التوست مع أول شاشة.

---

## ✅ خلص — المرحلة ٣ (الدخول) — جزء ١

- [x] **شاشة `/login`** — إدخال رقم الموبايل بحالاتها الـ٣ (فاضية / بيكتب / خطأ) + سبينر أثناء التحميل
- [x] أكشن الدخول `lib/actions/auth.ts` — يفرّق من الرقم: والد → `/pin` · عميل معروف → دخول مباشر · رقم جديد → `/register`
- [x] آلية دخول بدون كود تحقق (OTP) — باسورد مشتق على السيرفر من الرقم (قرار D-14)، جلسة حقيقية للـ RLS
- [x] مكوّنات مشتركة جديدة: `Button` · `InputField` (بحالاتها) + `cn()` في `lib/utils.ts`
- [x] حماية المسارات في الـ middleware — أي مسار غير الدخول محتاج جلسة، والمسجّل بيتحوّل من شاشات الدخول للرئيسية
- [x] `/register` و `/pin` مسارات جاهزة (stubs) بتستقبل الرقم — نبنيها في الجلسات الجاية
- [x] صورة اللوجو `public/images/logo-primary.png` (منزّلة من Figma، ١٢٩×١٣٥ في التصميم)
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضيف

- [x] **شاشة `/register`** — "نورتنا لأول مرة" (C-04→C-06) بحالاتها + التحقق إن الاسم عربي
- [x] أكشن `registerCustomer` — بيعمل العميل + حساب الدخول + الجلسة، ويعيد استخدام نفس آلية اللوجين
- [x] مكوّن مشترك جديد `BackButton` (زرار رجوع — بيتكرر في شاشات كتير)
- [x] `InputField` اتوسّع بخيار محاذاة رسالة الخطأ (يمين افتراضي / وسط في اللوجين)

- [x] **شاشة `/pin`** — الرقم السري للأدمن (A-04→A-06) بحالاتها (فاضي/متكتوب/خطأ)
- [x] مكوّن مشترك جديد `PinInput` (٦ خانات — أرقام عربية، خانة واحدة مخفية بتشغّل الكيبورد)
- [x] أكشن `verifyPin` + دالة قاعدة بيانات `verify_admin_pin` (bcrypt، مقفولة على السيرفر بس — T-17)
- [x] فصلت آلية الجلسة المشتركة في `lib/auth/session.ts` (عميل + أدمن)
- [x] توكن جديد `--text-h3` (٢٨px) + ملف مشترك لظلال الحقول `fieldShadows.ts`

> **باقي للمرحلة ٣ (بيتعمل مع أول شاشة عميل):** تركيب `<Toaster/>` في layout (customer) و(admin).
> **رقم الأدمن التجريبي = `01000000000` · PIN = ١٢٣٤٥٦**

---

## ✅ خلص — المرحلة ٤ (تطبيق العميل) — الرئيسية

- [x] **الصفحة الرئيسية `C-10→C-12`** (`/`) بحالاتها الـ٣: بيع مفتوح فاضي / بيع مفتوح + طلبات جارية / بيع مغلق
- [x] **كارت حالة البيع** `SaleStatusCard` — عدّاد تنازلي حي (يوم/ساعة/دقيقة/ثانية) + hook `useCountdown`؛ أخضر لما مفتوح، أحمر لما مغلق، والزر بيتطمس لما البيع مقفول
- [x] **الشِّل المشترك للعميل** — `layout (customer)` فيه `<Toaster/>` + `BottomNav` **مثبّت (`fixed`)** تحت (٣ تبويبات، شارة الطلبات الجارية) + مسافة سفلية للمحتوى
- [x] **الهيدر + السايدبار** `HomeHeader` + `Sidebar` **مطابق للتصميم C-13**: تحية باسم العميل + كارت «المستحق للسداد» (دين حقيقي محسوب) + تنقّل + الأسئلة المتكررة + تسجيل خروج، وخلفية مبلورة (`backdrop-blur`) — شاشة الديون المستقلة لسه
- [x] **`ContactButton`** كومبوننت «تواصل معنا» قابل لإعادة الاستخدام (شكل بس دلوقتي)
- [x] **أيقونات التاب بار** `NavIcon` — SVG مستخرج من Figma (خالد رسمها)، نشط = فيل / عادي = outline بنفس اللون؛ لأن Hugeicons المجانية stroke بس
- [x] **أيقونات السايدبار** مطابقة للتصميم بالظبط (ترس/مستخدم/باب/تليفون/border-full/محفظة معكوسة)
- [x] **بادينج الشاشة `px-screen`** توكن مرن (٢٤/٢٠/١٦) حسب عرض الشاشة — في `globals.css`
- [x] **queries جديدة** — `customers` (العميل الحالي، cached) · `cycles` (حالة البيع) · `orders` (عدد الجاري + الديون) · `notifications` (غير المقروء)
- [x] **أكشن تسجيل الخروج** `signOut` + شارة الجرس (الإشعارات غير المقروءة)
- [x] **حسم قرار المسارات (T-18):** العميل ياخد `/`، الأدمن تحت `/admin`، والتوجيه/الحماية حسب الدور في الـ middleware
- [x] stub لصفحة الأدمن `/admin` (لحد ما نبنيها في المرحلة ٥) + الـ PIN بيوجّه لـ `/admin`
- [x] `sale_closes_at` اتضاف للـ seed وللقاعدة الحيّة عشان العدّاد يبان
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضيف

> **شكل بس دلوقتي (مفيش وجهات):** «تواصل معنا» · «حول التطبيق» · «الأسئلة المتكررة» · ترس الإعدادات في السايدبار.

---

## ✅ خلص — المرحلة ٥ (تطبيق الأدمن) — الرئيسية أول مرة

- [x] **رئيسية الأدمن `A-10`** (`/admin`) — حالة «أول مرة، مفيش دورات»: تحية باسم صاحب المزرعة + رسمة الأرشيف الفاضي + زر «ابدأ سجل اول دورة»
- [x] **شِل تطبيق الأدمن** — `layout (admin)` فيه `<Toaster/>` + `AdminBottomNav` **مثبّت** تحت (٤ تبويبات: الرئيسية/الطلبات/العملاء/الدورات) + مسافة سفلية
- [x] **`AdminBottomNav` + `AdminNavIcon`** — البار السفلي للأدمن؛ التاب النشط بيتحدد **باللون بس** (أخضر غامق/باهت) مش بالتعبئة زي بار العميل (T-20) — أيقونات SVG مستخرجة من Figma
- [x] **`EmptyCyclesIllustration`** — رسمة الحالة الفاضية (currentColor، بلون الحدود)
- [x] **الترس (الإعدادات)** فوق يسار → `/admin/settings` (stub)
- [x] **queries جديدة** — `admin` (المزرعة الحالية + اسم صاحبها، cached) · `hasActiveCycle` (فيه دورة شغالة؟)
- [x] **حالة «فيه دورة شغالة»** بتوجّه لـ placeholder (لوحة الدورة A-11 شاشة جاية)
- [x] **stubs للمسارات:** `/admin/orders` · `/admin/customers` · `/admin/cycles` · `/admin/settings` — عشان التنقّل والأزرار تشتغل فعلاً
- [x] **الحماية** — الـ middleware بيأمّن `/admin/*` كله للأدمن بس (اتأكدنا، مفيش تعديل محتاج)
- [x] **عمود `owner_name`** اتضاف للـ farm (اسم صاحب المزرعة — D-15) — **الهجرة `005` اتطبّقت على القاعدة الحيّة والأنواع اتولّدت من جديد**، والاسم «صبري علي» ظاهر في التحية
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضيف

> **شكل بس دلوقتي (stubs):** الطلبات · العملاء · الإعدادات — نبنيهم في الجلسات الجاية.

---

## ✅ خلص — المرحلة ٥ — تسجيل الدورة A-41

- [x] **نافذة «إنشاء دورة جديدة» `A-41`** — bottom sheet بتطلع من تحت فوق خلفية مطموسة (نفس إحساس سايدبار العميل)
- [x] **`BottomSheet` مكوّن مشترك جديد** (ui) — سكرِم مطموس + انزلاق من تحت + إغلاق باللمس/Escape (T-21)
- [x] **`StatItem` مكوّن مشترك جديد** (ui) — كارت إحصائية (عنوان + قيمة)
- [x] **الحقول:** اسم الدورة · اختيار اليوم + الوقت (native pickers تحت شكل التصميم) · عدّاد عدد الكتاكيت + سعر الكتكوت (أرقام عربية + زر +)
- [x] **كارتين محسوبين حيّين:** «العلف المطلوب» (بادي/نامي) و«المصاريف المتوقعة» — **أرقام تقديرية مبدئية للمراجعة** (D-16)
- [x] **أكشن `createCycle`** — بيتحقق من عدد/سعر/تاريخ، ويرفض لو فيه دورة نشطة بالفعل (دورة واحدة نشطة — FR-4)؛ نجاح = توست، فشل = خطأ inline جوه النافذة
- [x] **حسابات جديدة:** `expectedFeedBags` (شكاير بادي/نامي) · `estimatedCycleExpenses` + ثابت `ASSUMED_FEED_BAG_PRICE` قابل للضبط
- [x] **الوصول:** صفحة `/admin/cycles` بقت فيها زر «إنشاء دورة جديدة» يفتح النافذة (وزر A-10 بيوصّل هناك)
- [x] **قاعدة البيانات:** عمودين للدورة `name` + `start_time` (الهجرة `006` اتطبّقت على الحيّة + الأنواع اتولّدت)
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضيف

> **⚠️ للمراجعة (D-16):** أرقام العلف (بادي ٠.٧٥ / نامي ٢.٧٥ كجم للفرخة) وسعر الشكارة المفترض (`ASSUMED_FEED_BAG_PRICE`) ومصطلحات بادي/نامي — كلها تقديرية، راجعها وقوللي الصح.
> **stubs باقية:** الطلبات · العملاء · الإعدادات · قائمة الدورات لما يكون فيه دورات (A-42+).

---

## ✅ خلص — المرحلة ٥ — الدورات فاضية A-40

- [x] **صفحة الدورات `A-40`** (`/admin/cycles`) في حالة «مفيش أي دورات» (لا حالية ولا قديمة): رسمة الأرشيف + نص + زر «ابدأ سجل اول دورة» بيفتح نافذة التسجيل مكانها
- [x] **query جديد `hasAnyCycle`** (فيه أي دورة خالص؟ — غير `hasActiveCycle`)؛ لو فيه دورات بيوري placeholder لقائمة الدورات (A-42 لسه)
- [x] **إعادة استخدام** `EmptyCyclesIllustration` + `CreateCycleLauncher` (مفيش تكرار)
- [x] تصحيح إملائي: «ابدء»→«ابدأ» · «إدرتها»→«إدارتها»
- [x] `pnpm build` + `pnpm lint` نضاف

> **تحسين مراجعة:** نقلت تحويل الأرقام عربي→لاتيني لـ `format.ts` (`toLatinDigits`) بدل تكراره جوه `NumberStepper`.

---

## ✅ خلص — المرحلة ٥ — لوحة الدورة في التربية A-11

- [x] **رئيسية الأدمن في حالة «دورة نشطة في مرحلة التربية» `A-11`** (`/admin`) — الهيدر (اسم الدورة + شارة «مرحلة التربية» + تاريخ البدء + عدد الكتاكيت) + ٣ كروت رئيسية (النافق/مصاريف الدورة/عمر الفراخ) + أزرار التسجيل + قسم العلف + شبكة تتبّع الاستهلاك + زر «بدء مرحلة البيع»
- [x] **الرئيسية بقت تتفرّع حسب طور الدورة** (T-22): مفيش دورة → A-10 · تربية → A-11 · بيع → A-20 (placeholder) · انتهت → A-21 (placeholder)
- [x] **موديل سحب العلف (D-17):** جدول جديد `feed_withdrawal` (شكارة = وحدة، سحب يدوي) — **الهجرة `007` اتطبّقت على القاعدة الحيّة + الأنواع اتولّدت** + بيانات سحب في الـ seed
- [x] **كومبوننتس جديدة (admin):** `CycleHeader` · `CycleStatCard` (كارت إحصائية بارز) · `FeedGrid` (شبكة مرنة ١٠/صف) · `CycleActionButton` (بيفتح نافذة placeholder) · `StartSellingButton` · `RaisingDashboard` (المُجمِّع)
- [x] **query جديد `getActiveCycleDashboard`** — بيجمّع الدورة + النفوق + المصاريف + العلف (شراء/سحب) في مكان واحد؛ كل الحسابات في `lib/calculations`
- [x] **حسابات علف جديدة:** `feedBagsPurchased` · `feedBagsWithdrawn` · `feedBagsAvailable` + `pluralizeDay` (جمع «يوم») في `format.ts`
- [x] **أيقونات جديدة:** `mortality` (skull) · `cycle` (eggs) · `expenseEdit` (note-edit) · `chick` (bird) · `calendarStart` — كلها متحقّق منها في Hugeicons
- [x] **توكن جديد `--shadow-stat`** لظل الكروت البارزة
- [x] البيانات التجريبية الحيّة: الدورة النشطة دلوقتي «دورة يوليو ٢٠٢٦» في مرحلة التربية (يوم ٣) — زوّدتها بعلف/نفوق/مصاريف واقعية عشان الشاشة تبان متسقة
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضيف

- [x] **تعديلات بعد المراجعة:** الترس بقى لوحده فوق وتحته عنوان الدورة (بدل ما كانوا في صف واحد) · عكست ترتيب الصفوف الأفقية الـ٣ (كروت/أزرار/علف) عشان تطابق التصميم في الـRTL · **شبكة العلف بقت تقويم أيام** (٤٠ يوم = الدورة كاملة، `CYCLE_TOTAL_DAYS`): كل مربع = يوم، ويتفعّل اليوم اللي اتسحبت فيه شكارة (`withdrawn_on − start_date`) — مش مربع لكل شكارة زي الأول

**نافذة «تسجيل نافق» A-14 (اتبنت فعلياً):**
- [x] **زر «تسجيل نافق» بقى يفتح نافذة منبثقة في نص الشاشة** (مش الـ bottom sheet) على خلفية رمادية مطموسة — مطابقة للتصميم (X أحمر + عنوان + حقل عدد أحمر + زر تسجيل)
- [x] **`Modal` مكوّن مشترك جديد** (ui) — نافذة مركزية + سكرِم (نظير `BottomSheet`)
- [x] **`RecordMortalityButton`** — الزر الأحمر + النافذة + استدعاء الأكشن (توست نجاح/فشل، النافذة تفضل مفتوحة لو فشل)
- [x] **أكشن `recordMortality`** — بيضيف نافق للدورة النشطة؛ العدد الحالي ونسبة النفوق بيتحدّثوا تلقائي (محسوبين على القراءة)
- [x] **`NumberStepper` اتوسّع بخيار `tone="danger"`** (خط سفلي + رقم أحمر) + **توكن `--shadow-modal`** + ستايلات أزرار الأكشن اتجمّعت في `cycleActionStyles.ts` (بدون تكرار)

> **شكل بس دلوقتي:** زر «بدء مرحلة البيع» بيتفعّل عند يوم ٣٠ (شاشة البيع A-20 جاية). «تسجيل مصاريف» A-15 و«سحب شكارة» A-13 بقوا شغّالين فعلياً.
> **⚠️ للمراجعة:** «العلف المطلوب» بيتعرض «بادي/نامي» مدوّر لأقرب شكارة (٤ / ١٤)؛ لسه على أرقام العلف التقديرية (D-16). وشلت الـ«ك» من «عدد الكتاكيت : ٤٥٠ ك» (كانت مبهمة) — عايزها ترجع؟

---

## ✅ خلص — تنظيم الكود + نافذة المصاريف A-15

**ريفاكتور تنظيمي:**
- [x] **زرار الأكشن بقى كومبوننت `ui/ActionButton`** (danger/outline + أيقونة + تحميل) — يتعاد استخدامه في أي مكان؛ شِلت `cycleActionStyles.ts`
- [x] **مجلد `components/admin` اتقسّم** لـ `home/` (لوحة الدورة) و`cycles/` (تسجيل/فاضي) — القاعدة: كل قسم أدمن له مجلد، وأي عنصر مشترك → `ui`
- [x] زرار الـX الأحمر الدائري بقى `CloseButton` المشترك في كل النوافذ

**نافذة «تسجيل مصاريف» A-15:**
- [x] **`ui/Chip` كومبوننت مشترك جديد** (شِب قابل للاختيار — يتعاد استخدامه: فئات/فلاتر/مواعيد) + توكن `--color-ink`
- [x] **`ExpenseSheet`** — bottom sheet فيه تبويبات فئات (علف/مياه وكهرباء/أدوية/أخرى) والفورم بيتغيّر حسب الفئة
- [x] **`FeedExpenseForm`** (فئة العلف): كروت العلف + «شكارة بادي/نامي» (عدد + سعر) → بيتسجّل في جدول `feed` (بيزوّد المتوفر)
- [x] **`SimpleExpenseForm`** (أدوية/أخرى): الحقل الأول بيتغيّر حسب الفئة (أدوية = «اسم العلاج» · أخرى = «التفاصيل») + السعر → جدول `expense`
- [x] **`UtilitiesExpenseForm`** (مياه وكهرباء): الكهرباء (بداية/نهاية العداد بالكيلو وات — اختياري — + سعر الفاتورة) + المياه (سعر الفاتورة) → كل فاتورة سطر مستقل في `expense` (قراءة العداد بتتحفظ في وصف سطر الكهرباء) · أكشن `addUtilitiesExpense`
- [x] **`RecordExpenseButton`** — الزر اللي يفتح النافذة (بيمرّر بيانات العلف للفورم)
- [x] **أكشنز `addFeedPurchase` + `addExpense`** (`lib/actions/expenses.ts`) — نجاح توست + تحديث اللوحة
- [x] **`InputField` اتوسّع بخيار `suffix`** (لـ«جنية» جوه الحقل)
- [x] `pnpm build` + `pnpm lint` نضاف

> **ملاحظة:** بادي/نامي في جدول `feed` مش متخزّنين كنوع منفصل (الجدول عدد شكاير + سعر بس) — التفرقة UI فقط. لو عايزين نفرّق التخزين نضيف عمود `kind` بعدين.

---

## ✅ خلص — نافذة «سحب شكارة» A-13

- [x] **زر «سحب شكارة» بقى بنفس عرض زر «تسجيل مصاريف» بالظبط** (مش عرض الشاشة كامل) — متحاذي تحته على اليمين، وبيفتح نافذة «امتي فتحت الشكارة؟» (Modal في نص الشاشة، مش bottom sheet)
- [x] **`RecordFeedWithdrawalButton`** (admin/home) — الزر + النافذة (حقل «اختار اليوم» + «اختار الوقت» — معبّيين باليوم/الساعة الحالية عشان حفظة بلمسة) + زر «حفظ»
- [x] **أكشن `addFeedWithdrawal`** (`lib/actions/expenses.ts`) — بيسجّل سطر في `feed_withdrawal` لليوم المختار (شكارة = وحدة)؛ بيتحقّق إن اليوم جوّه الدورة (مش قبل البداية ولا في المستقبل)
- [x] **الشبكة بتتفعّل:** أول ما تحفظ، مربّع اليوم اللي اختارته يولّع في «تتبع استهلاك العلف» والعلف المتوفر/المسحوب يتحدّثوا (توست نجاح + refresh)
- [x] **اتجاه الشبكة اتغيّر (D-17):** أول يوم بقى تحت على الشمال، والأيام بتمشي يمين→شمال في الصف السفلي وبتطلع لفوق (النهارده أحدث يوم فوق)
- [x] **تحسينات على مكوّنات مشتركة:** `ActionButton` بقى فيه variant «primary» (لون لايم — زر «حفظ») · `CloseButton` بقى فيه مقاس `sm` للنوافذ المنبثقة (أصغر من اللي في الـ bottom sheet)
- [x] **اتشال `CycleActionButton`** (كان placeholder — بقى مش مستخدم بعد ما الزرّين الحقيقيين اتبنوا)

**نافذة تفاصيل الشكارة (اتبنت):**
- [x] **دوس على أي مربّع متفعّل في الشبكة يفتح نافذة تفاصيل الشكارة** (node 3238:10980): رقم الشكارة + نوعها (بادي/نامي) + عمر الفراخ يومها + اليوم والساعة
- [x] **`FeedGrid` بقى تفاعلي** (client) — المربّعات المتفعّلة أزرار بتفتح النافذة · **`FeedWithdrawalDetail`** نافذة العرض
- [x] **نوع الشكارة بيتحسب أوتوماتيك (قاعدة خالد):** أول شكاير بعدد البادي المطلوب للدورة = بادي، اللي بعدهم = نامي (FIFO)
- [x] **الساعة بقت تتخزّن:** هجرة `008` ضافت عمود `withdrawn_at` لجدول `feed_withdrawal` (اتطبّقت على القاعدة الحيّة + الأنواع اتولّدت)؛ نافذة السحب بتبعت اليوم والساعة
- [x] `pnpm build` بيعدّي نضيف

---

## ✅ خلص — لوحة الدورة في البيع A-20

- [x] **رئيسية الأدمن في حالة «البيع مفتوح» `A-20`** (`/admin`) — الترس فوق + ٣ شارات (سعر الكيلو · «البيع متوفر» · عمر الفراخ) + ٣ أقسام كروت: احصائيات الفراخ · الاحصائيات المالية · الطلبات
- [x] **الرئيسية بقت بتفرّع لـ٤ حالات فعلياً:** مفيش دورة → A-10 · تربية → A-11 · **بيع → A-20** · انتهت → A-21 (لسه placeholder)
- [x] **ميزان الفراخ (D-18):** المتوفرة = عدد الكتاكيت − النافق − اللي اتباع (تم التسليم) − اللي متحجوز في طلبات شغالة · «تم بيعها» = المسلَّم بس · «المطلوبة» = طلبات لسه شغالة — التلاتة بيقسّموا القطيع من غير تكرار
- [x] **«اجمالي الدخل» مطمّس ويتكشف باللمس (D-19)** — الكارت كله هو الزرار (مساحة لمس ١٠٠px)
- [x] **الكروت بتعرض الرقم لوحده من غير «جنيه» (D-20)** — زي التصميم بالظبط؛ عنوان القسم «الاحصائيات المالية» هو اللي بيوضّح إنها فلوس. باقي التطبيق زي ما هو (`formatCurrency` لسه بتحط «جنيه»، وشارة السعر فوق كمان)
- [x] **queries جديدة:** `selling.ts` (`getSellingStats` — قراءة واحدة لكل طلبات الدورة + أسطرها + دفعاتها) · `settings.ts` (`getFarmSettings` — سعر الكيلو وباقي الإعدادات، cached)
- [x] **حسابات جديدة:** `sumInvoices` (الدخل/المحصّل/الديون) في `invoice.ts` · `availableChickens` + `averageChickenWeight` في `cycle.ts`
- [x] **أعداد الطلبات بتتحسب من `ADMIN_ORDER_TABS`** نفسها — الكروت وتبويبات الطلبات عمرهم ما هيختلفوا
- [x] **أيقونات جديدة متحقّق منها:** `chickensAvailable` (store-verified-02) · `chickensRequested` (timer-01) · `debt` (wallet-03) · `cash` (wallet-02) · `ordersProcessing` (knives) · `ordersNew` (border-full)

**تنظيم الملفات (T-23):**
- [x] **`components/admin/home` اتقسّم ٤ مجلدات:** `shared/` (المشترك بين اللوحات) · `raising/` (A-11/13/14) · `expenses/` (A-15→A-19) · `selling/` (A-20)
- [x] **كارت الإحصائية واحد للوحتين** — `CycleStatCard` اتوسّع بـ`tone` (أخضر/أحمر/بيچ/زيتوني) و`raised` (ظل من عدمه) و`blurred`، بدل ما نكرّر كارت تاني
- [x] **`ui/Badge` كومبوننت مشترك جديد** (٤ ألوان) — الشارات التلاتة + شارة «مرحلة التربية» في A-11 بقوا نفس المكوّن
- [x] **`layout/SettingsGear`** — الترس كان متكرر في ٣ شاشات، بقى مكوّن واحد
- [x] **`shared/StatSection`** — عنوان قسم + محتواه؛ A-11 استخدمه كمان في «تتبع استهلاك العلف»
- [x] تصحيح إملائي من التصميم: «جنية» → «جنيه» · «الطلبات الجديد» → «الطلبات الجديدة»
- [x] **توسيط الكلام العربي بصرياً (T-25):** خط Almarai بيحجز ٠.٩٠٥em فوق الخط الأساسي بس أطول حرف (ا/ل) بيوصل ٠.٧١٦em بس — الفرق ده مساحة للهمزات والنقط اللي فوق، ومعظم كلامنا مفيهوش. النتيجة إن أي كلام متوسّط رأسياً بيبان نازل تحت. ضفت متغيّر `--text-lift-ar` (٠.٠٩em = نص الفرق) + يوتيليتي `optical-center` وطبّقتها على الـ`Badge`. **لو لسه حاسس إنه نازل، غيّر الرقم ده لوحده في `globals.css`** (٠.١٢ أو ٠.١٤ الخطوة الجاية)
- [x] **إصلاح سُمك الأيقونات (T-24):** `<Icon>` كان بيفرض `strokeWidth={1.5}` على كل مسارات الأيقونة، ومكتبة Hugeicons بتحطه كمان على المسارات المتحوّلة لـfill — فأيقونة «الفراخ المتوفرة» (store-verified-02) كان جسم المحل فيها بيتلوّن بسُمك مضاعف جنب علامة الصح. شِلت الفرض؛ كل أيقونة بقت بتستخدم سُمكها الأصلي (١.٥ برضه) — باقي الـ٥٦ أيقونة متأثرتش
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضاف

> **إزاي تشوف الشاشة:** الدورة الحالية لسه في التربية — ادوس «بدء مرحلة البيع» في A-11 (اتفعّل خلاص لأن عمر الفراخ عدّى ٢٧ يوم) والرئيسية هتقلب على A-20 على طول.
> **⚠️ فجوة في التصميم:** مفيش أي زرار «إنهاء الدورة» في مرحلة البيع، يعني مفيش حاجة بتنقّل الدورة من «بيع» لـ«انتهت». محتاج قرار — الزرار ده مكانه A-20 ولا A-21؟

---

## ✅ خلص — نافذة تأكيد بدء البيع + تحديد السعر

- [x] **زر «بدء مرحلة البيع» بقى يفتح نافذة تأكيد الأول** (node 3608:3838) بدل ما يفتح البيع على طول: «هل انت متأكد من بدء البيع ؟» + **«سعر كيلو الفراخ؟»** + زرّين (بدء البيع / الغاء)
- [x] **السعر بيتحفظ في `settings.sale_price`** — يعني تقدر تغيّره بعدين من الإعدادات عادي زي ما طلبت، **والفواتير القديمة مبتتغيّرش** لأن سعر الكيلو بيتثبّت على الطلب وقت الوزن (T-15)
- [x] **النافذة بتفتح على السعر الحالي** — الحالة الطبيعية إنك تدوس «بدء البيع» على طول من غير ما تلمس أي حاجة
- [x] **ترتيب الحفظ مقصود (D-21):** السعر الأول وبعدين فتح البيع — لو فتح البيع فشل، البيع يفضل مقفول وتعيد المحاولة؛ العكس كان ممكن يفتح البيع بسعر قديم
- [x] **الفشل = خطأ ثابت جوه النافذة مش توست** — فتح البيع بيبان لكل العملاء، والتوست لو اختفى من غير ما تشوفه هتفضل مستني طلبات مش جاية (T-09)
- [x] **`ui/Stepper` كومبوننت مشترك جديد** — الـ`−  ٩٠  +` الكبير؛ الرقم input حقيقي (تقدر تكتب السعر بدل ما تدوس ٤٠ مرة) بس شكله رقم عادي زي التصميم بالظبط
- [x] **`AddButton` بقى `StepButton`** بخاصية `sign` (زائد/ناقص) — التصميم بيرسم العلامتين على نفس المربع اللايم، فكومبوننت واحد يكفي (و`NumberStepper` لسه شغال بالزائد)
- [x] **توكن جديد `--text-h2`** (٣٢px) لرقم السعر
- [x] `salePrice` اتضاف لـ`CycleDashboard` (قراءة واحدة cached) — النافذة وشارة السعر في A-20 بياخدوا منه
- [x] `pnpm build` + `pnpm lint` نضاف

---

## ✅ خلص — شاشة الطلبات A-50 (الهيكل + الحالات الفاضية)

- [x] **صفحة الطلبات `A-50`** (`/admin/orders`) — التولبار (فلتر الدورة + «اضافة طلب») + خانة البحث + شريط التبويبات بأعداد حقيقية + الحالة الفاضية لكل تبويب
- [x] **٣ حالات فاضية مطابقة للتصميم:** الجديدة (ساعة رملية) · قيد التشغيل (سكاكين) · المكتملة (كرتونة مسلّمة) — كل واحدة برسمتها ونصها
- [x] **التبويب المختار بيتحفظ في رابط الصفحة** (`?tab=new|active|done`) — يعني الصفحة كلها سيرفر من غير حالة في المتصفح، والرجوع/التحديث بيشتغلوا صح (T-02)
- [x] **الأعداد حقيقية من القاعدة** — قراءة واحدة لحالات طلبات الدورة و`tallyOrderTabs` بتوزّعهم على التبويبات حسب `ADMIN_ORDER_TABS` (نفس المصدر بتاع كروت A-20)
- [x] **الدورة اللي الشاشة بتفلتر عليها:** `getDefaultOrdersCycle` — الدورة الشغالة، ولو مفيش يبقى آخر دورة انتهت (زي ما طلبت)
- [x] **`ui/EmptyState` كومبوننت مشترك جديد** — أيقونة كبيرة جوه دايرة + سطر واحد؛ هيتعاد استخدامه في العملاء والدورات والسجل
- [x] **مجلد جديد `components/admin/orders/`** — `OrdersToolbar` · `OrdersSearchBar` · `OrderTabs` · `OrdersEmptyState` (نفس قاعدة T-23: كل قسم أدمن له مجلد)
- [x] **أيقونات جديدة متحقّق منها:** `filter` (نفس مسار التصميم بالظبط) · `addOrder` (layer-add) · `ordersWaiting` (timer-01)
- [x] **زر «اضافة طلب»** بيوصّل لمسار حقيقي `/admin/orders/new` (stub لحد ما نبني A-56)
- [x] **تنظيف تكرار:** أعداد التبويبات كانت متعرّفة مرتين باسمين مختلفين (`fresh/inProgress/completed` في `selling.ts` و`new/active/done` في التبويبات) — بقت نوع واحد `OrderTabCounts` بمفاتيح التبويبات، ولوحة البيع بقت تستخدمه
- [x] `pnpm build` + `pnpm lint` بيعدّوا نضاف

> **شكل بس دلوقتي (قرارك ١٨ أغسطس):** زرار الفلتر (القمع) — لسه مفيش تصميم للقائمة اللي هتفتح منه · خانة البحث — لسه مفيش كروت تتبحث فيها ولا تصميم لحالة «مفيش نتيجة».
> **الجاي في الشاشة دي:** كروت الطلبات بحالاتها المختلفة → قائمة اختيار الدورة → تبويبات الدورة القديمة (مدفوع / عليه فلوس / ملغي).
> **ملاحظة من التصميم:** «قيد التشغيل» مكتوبة بلون أغمق شوية من أختيها (`#1a443d` بدل `#3f6246`) — وحّدتها على لون النص العادي؛ لو مقصودة قولّي.

---

## ✅ خلص — نافذة «انشاء طلب باسم عميل» A-56

- [x] **النافذة بملء الشاشة** (نافذة بس شكلها صفحة) — بتفتح من زر «اضافة طلب» في شاشة الطلبات، وقايمة الطلبات فاضلة ورا وبترجع أول ما تقفلها
- [x] **اختيار العميل** — تكتب اسم أو رقم تليفون، تنزل قايمة قصيرة تدوس على العميل فيتحدد (تقدر تشيله وتختار غيره). الفلترة في المتصفح على عملاء المزرعة المحمّلين مع الصفحة — مفيش طلب للسيرفر مع كل حرف
- [x] **مربع «طلب يتيم»** (FR-13) — لما تعلّمه، خانة العميل بتتقفل والطلب بيتسجّل من غير عميل، وتربطه بعميل بعدين
- [x] **مربع «لحد تبع العميل؟»** — بيفتح حقل «الطلب باسم مين؟» والاسم بيتحفظ في عمود `on_behalf_of`؛ الفلوس والدين بيفضلوا على العميل المعروف
- [x] **باقي الفورم:** عدّاد الفراخ (نفس الـ`Stepper` بتاع نافذة بدء البيع) · مفتاح «التنظيف» · اختيار الوزن من ٥ شارات · حقل الملاحظات
- [x] **الأوزان بتتقرا من الإعدادات** (FR-5) — والقاعدة اتظبطت على الـ٥ اللي في التصميم (١.٥ / ١.٧٥ / ٢ / ٢.٢٥ / ٢.٥) بدل ٣؛ الـseed اتحدّث كمان
- [x] **أكشن `createOrder`** — بيسجّل الطلب بحالة «في الانتظار» على الدورة الشغالة + **سطر لكل فرخة** (D-13)؛ **السعر مش بيتثبّت هنا** لأنه بيتثبّت وقت الوزن (T-15). لو تسجيل الأسطر فشل، الطلب بيتمسح عشان مايفضلش طلب من غير فراخ
- [x] **نجاح = توست «تم تسجيل الطلب» + النافذة تقفل وأعداد التبويبات تتحدّث · فشل = خطأ ثابت جوه النافذة** (نفس أسلوب نافذة إنشاء الدورة)
- [x] **كومبوننتس مشتركة جديدة (ui):** `Checkbox` · `Toggle` · `WeightBadge` (رسمة الوزنة من التصميم) · `TextareaField`
- [x] **توسعات:** `BottomSheet` بقى فيه `size="full"` (نافذة تغطي الشاشة) · `Button` بقى فيه `variant="outline"` (كان الستايل موجود بس مفيش زرار بيستخدمه) · توكن جديد `--color-control-border`
- [x] **مسار `/admin/orders/new` اتشال** — بقت نافذة مش صفحة
- [x] `pnpm build` + `pnpm lint` + فحص الأنواع بيعدّوا نضاف

> **زر «تأكيد الطلب ووزن الفراخ»** بيحفظ الطلب بس دلوقتي (زي الزرار اللي فوقه) — لما نبني شاشة الوزن A-52 هيحفظ ويوديك عليها على طول.
> **قايمة نتايج البحث عن العميل مش في التصميم** — ضفتها لأن الحقل من غيرها مش هينفع، وإنت وافقت.
> **⚠️ راجع بصرياً:** مفتاح «التنظيف» — في التصميم الزرار الداير بيقف **يمين** لما يكون مفتوح؛ نفّذتها زي التصميم بالظبط، بس المعتاد في التطبيقات العربية إنه يقف شمال. قوللي لو تحب أعكسه.

---

## ✅ خلص — كارت الطلب (حالة «قيد المراجعة») + رقم الطلب

- [x] **كارت الطلب `A-50`** (node 3295:9568) بحالة الطلب الجديد: الشارة + رقم الطلب وتاريخه · اسم العميل وتليفونه + زرّين تواصل · العدد والوزن والميعاد · زرّين الأكشن
- [x] **رقم الطلب زي ما طلبت (D-24):** رقم الدورة + رقم الطلب جوه الدورة، يعني الطلب الرابع في الدورة الأولى = `#١٠٠٤`
- [x] **هجرة `009` + `010`** — عمودين رقم (`cycle.seq` و `orders.seq`) بيتملوا **أوتوماتيك من قاعدة البيانات** وقت التسجيل، بفهرس فريد عشان الرقم عمره ما يتكرر ولا يتغيّر بعد ما تقوله للعميل. **اتطبّقوا على القاعدة الحيّة + الأنواع اتولّدت من جديد**
- [x] **زرّين التواصل شغّالين فعلاً** — واتساب بيفتح المحادثة على رقم العميل (بيحوّل ٠١… لصيغة دولية)، والتليفون بيتصل
- [x] **`OrderStatusBadge` كومبوننت مشترك جديد** — نفس الشارة هتشتغل في تطبيق العميل كمان، والتسمية بتتغيّر حسب مين بيقرا
- [x] **`ContactLinks` كومبوننت مشترك جديد** — رسمتين مستخرجين من التصميم (واتساب مصمّت + سماعة)
- [x] **query جديد `listOrders`** — طلبات التبويب المختار، الأحدث الأول، وكل طلب بييجي جاهز للعرض
- [x] `pnpm build` + `pnpm lint` + فحص الأنواع بيعدّوا نضاف

> **تعديل على قرار قديم (D-03):** كنا متفقين إن الطلب المستني يبان «في الانتظار» عند الأدمن و«قيد المراجعة» عند العميل. التصميم النهائي كاتب **«قيد المراجعة»** في كارت الأدمن كمان، وإنت قلتها كدا برضه — فوحّدتها. **لو عايزها ترجع «في الانتظار» دي كلمة واحدة أغيّرها.**
> **الزرّين في الكارت («وزن الفراخ» و«الغاء الطلب») شكل بس** زي ما اخترت — كل واحد فيهم شاشة لوحده (A-52 و A-51).
> **حاجات مش في التصميم اضطريت أقررها:**
> · الطلب اللي إنت سجّلته مفيش له ميعاد → الكارت بيكتب «مش محدد»
> · الطلب اليتيم مفيش له عميل → مكان الاسم بيكتب «طلب يتيم» وزرّين التواصل بيختفوا
> · «لحد تبع العميل؟» → سطر صغير تحت التليفون «الطلب لـ فلان»
> · لو أوزان الطلب مختلفة (هيحصل لما نعمل شاشة طلب العميل) → «أوزان مختلفة»
>
> **عشان تشوف الكارت:** افتح شاشة الطلبات ودوس «اضافة طلب» وسجّل طلب — هيظهر تحت تبويب «الجديدة» على طول.

---

## ✅ خلص — نافذة إلغاء الطلب A-51 + كارت الطلب الملغي

- [x] **نافذة «هل انت متأكد من الغاء الطلب؟»** — عنوان + حقل «سبب الغاء الطلب» بخط أحمر تحته + زر الإلغاء الأحمر، مطابقة للتصميم
- [x] **السبب إجباري** — من غيره الإلغاء مابيحصلش؛ لأن الكارت بيعرضه بعد كدا، و«ليه العميل ده مخدش طلبه» دي بالظبط الحاجة اللي بتتنسي
- [x] **الفشل = خطأ ثابت جوه النافذة مش توست (T-09)** — الإلغاء عملية حرجة: لو التوست اختفى من غير ما تشوفه هتفتكر إن الطلب اتلغى وتفضل حاجز فراخ لعميل مش جاي
- [x] **كارت الطلب الملغي** — الشارة بقت حمراء مصمّتة «تم الغاء الطلب»، والصف بتاع العدد/الوزن/الميعاد والزراير بيتشالوا ومكانهم «سبب الغاء الطلب» + السبب اللي كتبته
- [x] **هجرة `011`** — عمود `cancel_reason` (اتطبّقت على القاعدة الحيّة + الأنواع اتحدّثت)
- [x] **أكشن `cancelOrder`** — بيغيّر الحالة ويسجّل الوقت والسبب. **مفيش حاجة بتتمسح** — الطلب بيحتفظ بأسطره وبرقمه
- [x] **الطلب الملغي بيروح لتبويب «المكتملة»** زي ما اخترت (D-25) — التبويب معناه «خلصت منه» مهما كانت النهاية
- [x] **لوحة البيع متأثرتش** — حسابات الفلوس والفراخ بتستبعد الملغي أصلاً، فكارت «المكتملة» في الرئيسية لسه بيعدّ المسلَّم بس
- [x] `pnpm build` + `pnpm lint` + فحص الأنواع نضاف

- [x] **القلم اللي جنب السبب بقى شغّال** — تدوس عليه تعدّل السبب (نافذة بنفس شكل نافذة الإلغاء)، والتعديل بيغيّر النص بس من غير ما يلمس تاريخ الإلغاء
- [x] **رسمة القلم اتستخرجت من التصميم** — قلم Hugeicons شكله مختلف تماماً، فبقى مكوّن SVG خاص زي الواتساب
- [x] **توست نجاح للاتنين** («تم الغاء الطلب» / «تم تعديل سبب الإلغاء») جنب تغيّر الكارت نفسه

> **زر «وزن الفراخ» لسه شكل بس** — شاشة الوزن A-52 هي اللي جاية.

---

## ⏭️ الجاي

- [ ] **باقي شغل السرعة (مؤجّل للمرحلة ٨):** التحقق من الجلسة محلياً بدل نداء شبكة في الـmiddleware (`getClaims` — محتاج مراجعة أمان) · دمج الرحلات المتسلسلة للقاعدة (٥–٦ رحلة في كل تنقّلة) · الـcold start بتاع Vercel المجاني (مش مستاهل دلوقتي)
- [ ] **شاشات تحميل لتطبيق العميل** — الأدمن خلص؛ شاشات العميل تاخد `loading.tsx` وهي بتتبني، مش دفعة واحدة بعدين

- [ ] المرحلة ٤ — باقي شاشات العميل: الطلب `C-20` → التتبّع `C-30` → تفاصيل الطلب `C-40` → السجل `C-50` → الإشعارات `C-15`
- [ ] شاشة الديون المستقلة (تفصيل C-13)
- [ ] المرحلة ٥ — تكملة الأدمن: الدورة خلصت `A-21`/`A-22` → العملاء `A-30` → الإعدادات `A-70` → الطلبات (الأخيرة)

---

## 🖼️ صور التطبيق

| الملف | المقاس المطلوب | المقاس الفعلي | فين يتستخدم | الحالة |
|---|---|---|---|---|
| `public/icons/icon-512.png` | ٥١٢×٥١٢ | ٥١٢×٥١٢ | أيقونة التطبيق على الموبايل | ✅ نهائية من Figma |
| `public/icons/icon-192.png` | ١٩٢×١٩٢ | ١٩٢×١٩٢ | نفس الأيقونة، مقاس أصغر | ✅ نهائية من Figma |
| `public/icons/icon-maskable-512.png` | ٥١٢×٥١٢ | ٥١٢×٥١٢ | أندرويد — الرسمة جوه **٨٠٪** من المربع لأنه بيقص الحواف | ✅ نهائية من Figma |
| `public/icons/apple-touch-icon.png` | ١٨٠×١٨٠ | **٥١٢×٥١٢** | آيفون — من غير حواف دايرية، هو بيعملها | ✅ شغّالة (بس شوف الملاحظة) |

> **ملاحظة على `apple-touch-icon.png`** — الملف ده **نسخة طبق الأصل من `icon-512.png`** (نفس البصمة بالظبط)، يعني ٥١٢×٥١٢ بدل ١٨٠×١٨٠.
> **مش هيكسر حاجة** — الآيفون بيصغّرها لوحده والأيقونة هتبان صح. الفرق الوحيد إن حجمها ٢٠٩ كيلو بدل ~٢٥ كيلو، فأول تثبيت على الآيفون بيحمّل زيادة على الفاضي.
> لو حبيت تظبطها: صدّر ١٨٠×١٨٠ بنفس الاسم — مفيش كود هيتغيّر.

> **لـ Claude Code:** لما تحتاج صورة مش موجودة، ضيف سطر هنا باسم الملف والمقاس بالظبط، وحط placeholder في الكود.

## ❓ محتاج قرار من خالد

**١. مين بينهي الدورة؟** مفيش في التصميم أي زرار «إنهاء الدورة» في مرحلة البيع (A-20)، فالدورة مش هتقدر توصل لحالة «انتهت» (A-21) أبداً. اختار:
- **أ)** زرار «إنهاء الدورة» تحت في A-20 (زي «بدء مرحلة البيع» في A-11)
- **ب)** يتقفل تلقائي لما «الفراخ المتوفرة» توصل صفر (وده اللي FR-11 بيقوله)
- **ج)** نأجّله لحد ما نبني A-21 ونقرر ساعتها

**٢. كروت الطلبات التلاتة تفتح شاشة الطلبات؟** دلوقتي أرقام للعرض بس (زي التصميم بالظبط). لو عايز، كل كارت يوصّل لتبويبه في `/admin/orders`:
- **أ)** سيبها عرض بس (زي التصميم)
- **ب)** خليها تفتح تبويبها في شاشة الطلبات

> **لـ Claude Code:** أي سؤال هنا لازم يكون بخيارات جاهزة (أ / ب / ج)، مش سؤال مفتوح.

---

## 🐛 مشاكل معروفة

_(فاضي)_

---

## ✅ خلص — إصلاح الحراسة والتحويلات (١٩ أغسطس)

**١. الحارس كان مقفول تماماً — وده كان الأخطر.**
`middleware.ts` كان قاعد في جذر المشروع والتطبيق في `src/`. سيرفر التطوير كان **بيتجاهله في صمت**: مفيش تحذير، مفيش سطر ترجمة، مفيش خطأ. و`pnpm build` كان لسه بيسجّله في الـmanifest — فكل حاجة شكلها موصولة، وفي الحقيقة **أي مسار محروس كان بيرد كأن مفيش حارس أصلاً**. طلب من غير جلسة على `/admin` كان بيرجّع صفحة الأدمن ٢٠٠.

- **مفيش حاجة تسرّبت** — كل صفحة أدمن بتسأل `getCurrentFarm()` وبترمي على `/logout` لو مفيش مزرعة. بس ده صف الدفاع التاني، وكان شايل الحمل كله لوحده.
- **الحل:** الملف اتنقل لـ`src/proxy.ts` — وأول ما اتنقل اشتغل من أول طلب، وطلع تحذير نكست إن الاسم اتغيّر من `middleware` لـ`proxy` في نسخة ١٦، فاتسمّى بالاسم الجديد.
- **متأكد بالقياس:** `/admin` · `/admin/orders` · `/admin/customers` · `/` · `/order` · `/tracking` كلهم بقوا `307 → /login` للزائر من غير جلسة، و`/login` لسه ٢٠٠، وملف الـPWA لسه بيتحمّل عادي (٢٠٠) عشان التثبيت ميقفش.

**٢. التحويل لـ`0.0.0.0` (اللي إنت شوفته).**
`/logout` كان بيبني عنوان التحويل من `request.url` — وده العنوان اللي **السيرفر متربط عليه**، يعني `http://0.0.0.0:3000`. و`0.0.0.0` معناها «كل الكروت الشبكية»، مش مكان يتراح له.

- **الحل:** ملف جديد `src/lib/redirect.ts` بيبعت مسار نسبي (`/login`) — المتصفح بيركّبه على العنوان اللي هو واقف عليه، فيطلع صح من اللاب توب ومن الموبايل ومن Vercel.
- **الـproxy مش محتاجه** — نكست بيحوّل تحويلات الـproxy لمسار نسبي لوحده. القرار كامل في T-38.
- **مش الـdev script** — `-H 0.0.0.0` هو نفسه الافتراضي بالظبط، شيله أو سيبه مش هيفرق في المشكلة دي.

**٣. `pnpm dev` من غير `-H 0.0.0.0`** — الفلاج اتشال لأنه مالوش لازمة، والفايدة إن نكست بقى يطبع الـIP الحقيقي في سطر `Network` بدل `0.0.0.0`، وده اللي محتاجه وإنت بتجرب على الموبايل.

## 📊 عدّاد الشاشات

| التطبيق | خلص | الإجمالي |
|---|---|---|
| العميل | ١ (الرئيسية) | ~٣٥ |
| الأدمن | ٥ (الرئيسية أول مرة + لوحة التربية + **لوحة البيع** + تسجيل الدورة + الدورات فاضية) | ~٥٥ |

---

## ✅ خلص — المرحلة ٥ — شاشة العملاء (الحالة الفاضية) A-30

- [x] **شاشة العملاء `A-30`** (`/admin/customers`) — الهيكل كامل + حالة «مفيش أي عملاء»: زر «اضافة عميل» + «اجمالي الآجل» + صف الفلاتر + حقل البحث + الرسمة الدايرة ونص «لا يوجد اي عملاء حاليا»
- [x] **مكوّنات مشتركة جديدة (شِلت التكرار — T-28):**
  - `ui/AddButton` — زر الاضافة الأخضر الواحد (اضافة طلب / اضافة عميل)
  - `ui/SearchField` — حقل البحث الواحد؛ **كان مرسوم ٣ مرات** (الطلبات + اختيار العميل جوه نافذة الطلب + العملاء) وبقى ملف واحد
  - الاتنين لهم وضعين: شغّال (لو بعتله `onClick`/`onChange`) أو شكل بس — فشاشة لسّا مالهاش نافذة بتوري الشكل الصح من غير لمسة فاضية
- [x] **شاشة الطلبات اتنقلت على المشترك** — `OrdersSearchBar` اتشالت، و`AddOrderLauncher` بيستخدم `AddButton`، و`CustomerPicker` بيستخدم `SearchField`
- [x] **كومبوننتس الشاشة:** `CustomersToolbar` (الزر + اجمالي الآجل بأيقونة محفظة معكوسة) · `CustomersFilterBar` (عداد العملاء + فلتر الآجل)
- [x] **فلتر «الآجل» شغّال فعلاً** — رابط بيحط `?debt=1` (زي تبويبات الطلبات — D-26)؛ رمادي = مقفول، بنّي = مفتوح (A-31)
- [x] **query جديد `listCustomersWithDebt`** — كل عميل + اللي عليه (محسوب على القراءة من كل طلباته عبر الدورات)؛ الطلبات اليتيمة (FR-13) مش محسوبة على حد
- [x] **`pluralizeCustomer`** في `format.ts` — ١ عميل · ٢ عميلين · ٣–١٠ عملاء · ١١+ عميل (والصفر = «٠ عملاء» زي التصميم)
- [x] **أيقونة جديدة `addCustomer`** (نفس رسمة layer-add بتاعة اضافة طلب — التصميم بيستخدم نفسها)
- [x] `pnpm build` + `pnpm lint` نضاف

> **ملاحطة للمراجعة:** البيانات التجريبية فيها ٥ عملاء، فالحالة الفاضية مش بتبان دلوقتي — مكان الكروت بيوري «كروت العملاء قيد الإنشاء…» لحد ما تبعتلي شكل الصف.
> **باقي في الشاشة:** صف العميل (الجلسة الجاية) · نافذة تسجيل عميل A-34 · صفحة العميل A-33 · تفعيل البحث

---

## ✅ خلص — المرحلة ٥ — صف العميل A-30 (مقفول + مفتوح)

- [x] **صف العميل `CustomerRow`** بحالته الافتراضية: رقم الصف + الاسم · رقم التليفون + زر الاتصال + واتساب · الآجل بأيقونة المحفظة — وفاصل تحت الصف على عرض الشاشة بالكامل
- [x] **الصف بيتوسّع باللمس** (`3281:6260`): «طلبات الدورة» + «إجمالي الطلبات» · «إجمالي» و«مدفوع» بالجنيه · **شريط نسبة المدفوع** (أخضر = إجمالي الفاتورة، بنّي = المدفوع، بيمتلي من الشمال زي التصميم — T-29)
- [x] **لمسة تانية على الجزء اللي فوق = يقفل** · لمسة على الجزء المفتوح تحت **هتوصّل لسجل طلبات العميل** (شاشة لسه) — فمقفولة دلوقتي، وكذلك **القلم** (نافذة تعديل العميل A-35 لسه) — D-27
- [x] **قلم التعديل** بيظهر بس لما الصف يكون مفتوح، زي التصميم
- [x] **إعادة استخدام مش تكرار:** `shared/ContactLinks` (واتساب + اتصال) و`shared/PenGlyph` اللي كانوا مبنيين لشاشة الطلبات اتعادوا استخدامهم هنا
- [x] **تصحيح في `ContactLinks`** — زر الاتصال بقى قبل واتساب (كان معكوس في التصميمين، بما فيهم كارت الطلب — T-30)؛ و`PenGlyph` بقى ليه خيار `flipped` عشان كل شاشة تحطه في اتجاهه الصح
- [x] **query اتوسّع لـ `listCustomerSummaries`** — لكل عميل: الآجل + إجمالي الفواتير + المدفوع + عدد طلباته كلها + عدد طلباته في الدورة الحالية (كله محسوب على القراءة — D-05)
- [x] **`pluralizeOrder`** في `format.ts` — ١ طلب · ٢ طلبين · ٣–١٠ طلبات · ١١+ طلب (التصميم كان مكتوب «2 طلب»)
- [x] رقم الصف بيتكتب **«٢.»** مش «-2» (إصلاح المرحلة ٧)
- [x] `pnpm build` + `pnpm lint` نضاف

> **⚠️ محتاج تأكيد منك:**
> ١. **شريط المدفوع:** أخذت البنّي = المدفوع (لأنه واقف تحت كلمة «مدفوع») والأخضر = إجمالي الفاتورة. تحب نعكس الألوان (أخضر = المدفوع)؟
> ٢. **رقم الصف** خليته على اليمين قبل الاسم زي التصميم، بصيغة «٢.».
> **باقي:** نافذة تسجيل عميل A-34 · نافذة تعديل عميل A-35 · سجل طلبات العميل · تفعيل البحث

**تعديلات بعد المراجعة (صف العميل):**
- [x] **حقل البحث** بقى ياخد نفس **الظل الأخضر** بتاع أي حقل عادي وهو بيتكتب فيه (`FIELD_ACTIVE_SHADOW_WITHIN` — نفس المستخدم في `InputField`)
- [x] **مفيش آجل = أخضر** — المحفظة والمبلغ بيبقوا أخضر بدل البنّي لما الآجل يكون صفر، في صف العميل **وفي الإجمالي فوق** (D-28). اتعملوا في مكوّن واحد `DebtAmount` عشان ميتكرروش
- [x] **زرار الاتصال وواتساب بقوا عمود واحد في كل الصفوف** — رقم التليفون بقى في خانة بعرض ثابت (`11ch`)، لأن أرقام خط Almarai مش كلها بنفس العرض فكانت بتزقّ الزرارين يمين وشمال شوية كل صف
- [x] **ألوان الشريط اتعكست** زي ما طلبت: **الأخضر = المدفوع** والبنّي = الباقي (T-29)

---

## ✅ خلص — المرحلة ٥ — تسجيل وتعديل العميل A-34 + A-35

- [x] **نافذة واحدة للاتنين** `CustomerSheet` — النافذتين في Figma متطابقتين تماماً ما عدا العنوان واسم الزر والقيم المبدئية، فبقوا مكوّن واحد: تبعتله عميل = تعديل، متبعتوش = تسجيل جديد (D-29)
- [x] **زر «اضافة عميل»** فوق بقى شغّال فعلاً → يفتح نافذة التسجيل (A-34)
- [x] **قلم التعديل** في الصف المفتوح بقى شغّال → يفتح نافذة التعديل (A-35) وفيها بيانات العميل جاهزة
- [x] **أكشنز جديدة** `lib/actions/customers.ts` — `addCustomer` و `updateCustomer`:
  - الاسم **بالعربي** وعلى الأقل حرفين (نفس قاعدة شاشة تسجيل العميل بنفسه)
  - الرقم **١١ رقم** بالظبط، والحقل بيمنع أي حاجة غير أرقام وهو بيتكتب
  - **الرقم مش بيتكرر في نفس المزرعة**، ولو مكرر الرسالة بتقول **مين** اللي عنده الرقم: «الرقم ده مسجّل بالفعل لـ محمود الخياط»
- [x] **نجاح = توست + القائمة تتحدّث** · **فشل = خطأ inline جوه النافذة** والنافذة تفضل مفتوحة واللي اتكتب زي ما هو (مش توست — عشان ميضيعش قبل ما يقراه ويعيد كتابة الحقلين)
- [x] **مساحة لمس القلم بقت ٤٤px** بحدود شفافة (مش padding — الـpadding كان هيمدّد سطر الاسم ويلخبط الصف)
- [x] **إصلاح مهم في `BottomSheet`** — خاصية `pointer-events` بتتوارث في CSS، والقلم موجود جوه طبقة `pointer-events-none` (اللي بتخلّي اللمس يعدّي للزر اللي بيفتح ويقفل الصف)، فالنافذة كانت هتظهر تمام و**متستقبلش أي لمسة**. النافذة بقت بتحدد الخاصية بنفسها (T-31)
- [x] **`AddButton` اتبسّط** — شلت وضع «شكل بس» (مبقى مستخدم بعد ما الزرارين الاتنين بقوا شغّالين)
- [x] `pnpm build` + `pnpm lint` نضاف

> **باقي في شاشة العملاء:** صفحة/نافذة سجل طلبات العميل (لما تضغط على الجزء المفتوح تحت) · تفعيل البحث

**تفعيل البحث (A-30):**
- [x] **حقل البحث بقى شغّال فعلاً** — بيدوّر بالاسم أو برقم الهاتف، والنتيجة بتبان وهو بيكتب (فلترة في المتصفح على القائمة المحمّلة أصلاً — مفيش طلب للسيرفر مع كل حرف — T-32)
- [x] **البحث بيفهم العربي زي ما بيتكتب مش زي ما بيتهجّى** — «احمد» بتلاقي «أحمد» · «فاطمه» بتلاقي «فاطمة» · والتشكيل مبيفرقش
- [x] **البحث بالأرقام العربية كمان** — لو كتب «٠١٠٢» بيلاقي الرقم اللي مخزّن `0102…`
- [x] **نفس منطق البحث** اتشال من جوه `CustomerPicker` (نافذة إضافة طلب) وبقى في ملف واحد `lib/search.ts` بيستخدمه الاتنين
- [x] **عدّاد العملاء فوق بيتبع النتيجة** — زي A-31 بالظبط
- [x] **مفيش نتيجة** → «مفيش عميل بالاسم او الرقم ده» (مفيش تصميم للحالة دي، فأخدت شكل الحالة الفاضية بنص مختلف)
- [x] `pnpm build` + `pnpm lint` نضاف

> **باقي في شاشة العملاء:** سجل طلبات العميل (اللمس على الجزء المفتوح تحت) بس.
> **ملاحظة:** حقل البحث في **شاشة الطلبات** لسه شكل بس — تفعيله محتاج قرار: نبحث بالاسم ورقم الطلب سوا؟ وشكل «مفيش نتيجة» هناك؟
- [x] **آخر صف في قائمة العملاء مبقى ليهوش بوردر تحت** — الفاصل اتنقل من الصف نفسه للقائمة (`divide-y`)، فبيتحط بين الصوف بس ومش بيسيب خط ناقص تحت آخر واحد
- [x] **البحث بقى أسمح في العربي** (نفس المنطق بيخدم نافذة إضافة طلب كمان):
  - **«ال» التعريف مبقى يفرقش** — «الشيخ احمد» تلاقي «شيخ احمد» والعكس
  - **ترتيب الكلام مبيفرقش** — «احمد الخياط» تلاقي «الخياط احمد»
  - **المسافة مبتفرقش** — «عبدالله» تلاقي «عبد الله» والعكس
  - **الهمزة والألف والياء والتاء المربوطة** كلهم شكل واحد: احمد=أحمد · مصطفي=مصطفى · فاطمه=فاطمة · اسما=اسماء
  - التشكيل والتطويل مبيفرقوش
  - اختبرته على ١٧ حالة (منها حالات **مفروض متظهرش**) وكلهم عدّوا صح

---

## ✅ خلص — المرحلة ٨ (جزء ١) — التطبيق بقى يتثبّت على الموبايل (PWA أساسي)

- [x] **`src/app/manifest.ts`** — ملف الـmanifest، بس **مكتوب كود مش JSON ساكن** (T-33): لو فيه مفتاح غلط، البيلد بيقع فوراً بدل ما زرار «تثبيت» يختفي من الموبايل من غير ما حد يعرف ليه
- [x] **`display: standalone`** — بعد التثبيت التطبيق بيفتح **من غير شريط المتصفح خالص**: مفيش رابط ولا تابات، شاشة كاملة زي أي تطبيق
- [x] **أيقونات التطبيق الأربعة** في `public/icons/` (١٩٢ · ٥١٢ · maskable للأندرويد · apple-touch للآيفون) — **النهائية من Figma** (الجدول فوق)
- [x] **الآيفون مغطّى بشكل منفصل** — سفاري بيتجاهل الـmanifest أصلاً، فالمعلومات نفسها اتكتبت تاني في `appleWebApp` جوه الـlayout
- [x] **اسم الشاشة + لون شريط الحالة + شاشة البدء** كلهم على `#fbfdfc` (نفس خلفية التطبيق)
- [x] **`formatDetection: telephone: false`** — الآيفون كان هيحوّل أي أرقام لروابط تليفون زرقا (والأرقام عندنا أوزان وأسعار وأرقام طلبات)؛ أرقام التليفون الحقيقية ليها زراريرها أصلاً
- [x] **الـsafe-area كانت متظبطة أصلاً** في كل الشاشات والبار السفلي — اتراجعت، مفيش تعديل محتاج
- [x] `pnpm build` بيعدّي نضيف و`/manifest.webmanifest` بيتولّد

> **الشغل أوفلاين (Service Worker) متأجّل عن قصد لآخر المرحلة ٨ — القرار T-33.** التطبيق كله بيانات حيّة (طلبات/أوزان/فلوس)؛ لو خزّنّا الصفحات في الكاش دلوقتي، ممكن الأدمن يفتح التطبيق ويشوف **طلب اتلغى** أو **رصيد قديم** ويتصرّف على أساسه — وده أخطر من إنه ميشتغلش أوفلاين.
> **باقي في المرحلة ٨:** Service Worker · بانر «ثبّت التطبيق» (FR-2) · الإشعارات داخل التطبيق (FR-31) · التجربة على موبايل الوالد.

**الديبلوي على Vercel:**
- [x] الكود مرفوع على GitHub وجاهز — الريبو متوصّل، وأي `push` بعد كدا بينزل على الموقع لوحده
- [ ] **خطوة خالد (من المتصفح):** ربط الريبو بـVercel + إدخال الـ٣ متغيرات (`NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` · `SUPABASE_SECRET_KEY`)

> **⚠️ قبل ما الرابط يشوفه ناس بره:** الدخول بالتليفون بس من غير كود تحقق (D-14) — أي حد يعرف رقم مسجّل يقدر يدخل مكانه، وPIN الأدمن لسه ١٢٣٤٥٦. البيانات دلوقتي كلها تجريبية فمفيش خطر، بس دي محادثة لازم تحصل قبل ما الوالد يشتغل عليه ببيانات حقيقية.

**🐛 إصلاح بعد أول ديبلوي — لفة ريديركت لا نهائية عند دخول الأدمن (T-34):**
- [x] **السبب:** حساب الأدمن كان الدور بتاعه (`role: admin`) متسجّل في `user_metadata` بدل `app_metadata` — كتبته نسخة قديمة من الكود أول ما الحساب اتعمل، والـmiddleware بيقرا من `app_metadata` بس (D-14، لأنها الخانة اللي المستخدم ميقدرش يعدّلها). فالدخول بينجح والحساب مايبقاش لا أدمن ولا عميل
- [x] **اللفة:** الـmiddleware يوديه لتطبيق العميل → تطبيق العميل مايلاقيش له حساب عميل فيوديه لـ`/login` → الـmiddleware يشوف إن فيه جلسة فيوديه لتطبيق العميل → وهكذا للأبد، **ومفيش شاشة يدوس عليها**
- [x] **اتصلّح في القاعدة** — الدور اتنقل لمكانه الصح في حساب الأدمن
- [x] **اتصلّح في الكود عشان ميتكررش:** `signIn` بقى يصلّح الدور وهو بيعدّي (ويعيد تسجيل الدخول عشان الجلسة الجديدة تحمله فعلاً) — مبيعملش حاجة على الحسابات السليمة
- [x] **اتصلّح باب الخروج:** مسار `/logout` بيمسح الجلسة وبعدين يوديك للدخول؛ أي شاشة تلاقي جلسة من غير بيانات بتوديك هناك بدل `/login`. والـmiddleware بينهي أي جلسة مالهاش دور بدل ما يخمّن مكانها
- [x] **الـmanifest اتشال من حماية المسارات** — المتصفح بيطلبه من غير كوكيز، فالحماية كانت هتردّ عليه بتحويل لـ`/login` والتطبيق مكانش هيتثبّت

**🐛 إصلاح بعد الديبلوي — «الشاشة كلها بتتسحب» (T-35):**
- [x] **السبب الحقيقي مكانش سكرول أصلاً** — الشِل كان مقاسه `dvh` («طول الشاشة دلوقتي»)، فأول ما كروم يخفي شريط العنوان بتاعه الشاشة بتكبر ~٦٠px والشِل بيكبر معاها، والمحتوى اللي تحت التاب بار بيطلع فجأة. اتغيّرت لـ`svh` (طول ثابت مبيتغيّرش) في كل مكان: شِل الأدمن · شِل العميل · شاشات الدخول · الـbody · النوافذ اللي بتطلع من تحت
- [x] **`<html>` اتشال منه `h-full`** — كان بيخلّي الصفحة أطول من الشاشة بقد شريط العنوان
- [x] **قاعدة جديدة: صندوق سكرول واحد في الشاشة** — مفيش `overflow-y-auto` جوه `<main>` تاني. اللي المفروض يفضل ثابت (تولبار/فلاتر/بحث/تبويبات) بيتحط في بلوك `sticky` فوق، والصفوف بتعدّي من تحته. اتطبّقت على **شاشة الطلبات A-50** و**شاشة العملاء A-30**
- [x] **`overscroll-contain`** على `<main>` — السحبة اللي بتعدّي آخر صف مبقتش توصل للمتصفح
- [x] **`no-scrollbar`** يوتيليتي جديد في `globals.css` — شريط السكرول الرمادي اتخفى من الصفوف اللي بتتسحب يمين وشمال (شارات الوزن · تبويبات الطلبات · فئات المصاريف)

> **مهم للشاشات الجاية:** أي شاشة فيها قايمة لازم تتبع القاعدة دي (التفاصيل في T-35) — الطريقة القديمة شكلها سليم على اللاب توب وبتغلط على الموبايل بس.
- [x] **النوافذ بقت تظهر فوق التاب بار (T-36)** — بعد ما التولبار بقى `sticky` (T-35)، زراير «اضافة طلب» و«اضافة عميل» بقوا جواه، والنافذة اللي بتفتح منهم كان ترتيبها بيتحسب **جوه الهيدر** فالتاب بار بيغطيها. النوافذ (`BottomSheet` + `Modal`) بقت بتترسم في `<body>` مباشرة (portal) — يعني خرجت من ترتيب أي عنصر فوقها، ودي الطريقة الوحيدة اللي تفضل صح مهما اتحط الزرار فين
- [x] **hook جديد `useIsHydrated`** — بيقول للمكوّن إن المتصفح استلم، عشان الـportal ميحاولش يشتغل على السيرفر

---

## ✅ خلص — شاشات التحميل (`loading.tsx`) لكل مسارات الأدمن

**المشكلة:** التنقّل بين الشاشات كان بياخد ثانية لـ٥ ثواني و**من غير أي رد فعل** — Next بيفضل واقف على الشاشة القديمة لحد ما الجديدة تجهز بالكامل. الوالد لو دَس ومحصلش حاجة هيدوس تاني.

- [x] **مكوّن `ui/Skeleton`** — البلوك الرمادي الواحد (شريط · مربّع · دايرة). المقاس والشكل بييجوا من اللي بينده عليه، لأن السكيليتون مالوش قيمة غير لما يشبه الحاجة اللي واقف مكانها
- [x] **مكوّن `ui/SkeletonScreen`** — قاعدة شاشة التحميل. شايل الإعلان الوحيد اللي قارئ الشاشة محتاجه (`جاري التحميل`)، والبلوكات نفسها `aria-hidden` — فبيتقري مرة واحدة مش يعدّ عشر مربعات فاضية
- [x] **توكن لون جديد `--color-skeleton`** في `globals.css` (بيشاور على `--color-border`) — مفيش لون متكتوب بالإيد
- [x] **النبضة بتقف مع `prefers-reduced-motion`** — اللي طالب من موبايله حركة أقل بياخد الهيكل من غير نبض

**خمس شاشات تحميل، كل واحدة بنفس هيكل شاشتها بالظبط:**

| الملف | بيرسم إيه |
|---|---|
| `(admin)/admin/loading.tsx` | لوحة الدورة: الهيدر + التلات كروت + زراير التسجيل + صف العلف + قسم بعنوان |
| `(admin)/admin/orders/loading.tsx` | الهيدر المثبّت (اضافة + الفلتر · البحث · التبويبات) + تلات كروت طلبات |
| `(admin)/admin/customers/loading.tsx` | الهيدر المثبّت (اضافة + اجمالي الآجل · العدّاد والآجل · البحث) + ٦ صفوف بفواصلهم |
| `(admin)/admin/cycles/loading.tsx` | الدايرة في النص + سطرين + الزرار تحت |
| `(admin)/admin/settings/loading.tsx` | سطرين في النص |

> **ليه الإعدادات ليها ملف لوحدها؟** من غيره كانت هتورث شاشة تحميل الرئيسية — يعني تدوس على الترس فيلمع لك هيكل لوحة دورة على طريقك لشاشة شكلها مختلف تماماً.

> **الرئيسية ليها تلات وشوش** (تربية · بيع · مفيش دورة) ومحدش يعرف الجاي أنهي واحد، فالسكيليتون بيرسم الشكل المشترك بين لوحتَي التربية والبيع — وده اللي بيوصل فعلاً على مزرعة فيها دورة شغالة، وهي الحالة العادية.

- [x] **مكسب إضافي مجاني:** Next مش بيحمّل المسار الديناميكي مقدماً غير لما يلاقي `loading.tsx` جنبه — يعني التنقّل من البار السفلي بقى فوري فعلاً، مش بس شكله فوري
- [x] `pnpm build` + `pnpm lint` عدّوا نضاف

> **باقي:** شاشات العميل لسه من غير `loading.tsx` — هتاخدها وهي بتتبني، مش دفعة واحدة بعدين.
